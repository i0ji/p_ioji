import clsx from "clsx";

export default function Portfolio() {
  return (
    <div
      id="portfolio"
      className={clsx(
        "h-screen snap-start",
      )}
    >
      github/i0ji
    </div>
  );
}
