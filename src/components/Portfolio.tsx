import clsx from "clsx";

export default function Portfolio() {
  return (
    <section
      id="portfolio"
      className={clsx(
        "h-screen snap-start",
      )}
    >
      github/i0ji
    </section>
  );
}
